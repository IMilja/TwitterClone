<nav class="navbar navbar-expand-sm navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand" href="{{ path('default.index') }}">{{ 'twitter clone' | title }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item active">
          <a class="nav-link" href="{{ path('default.index') }}">Home</a>
        </li>
        {% if not is_granted('IS_AUTHENTICATED_REMEMBERED') %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('security.app_login') }}">Log in</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('security.app_register') }}">Sign up</a>
          </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path("security.app_logout") }}">Log out</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>